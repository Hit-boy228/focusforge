// tests/integration/postgres/session_repository_it.cpp
#include <gtest/gtest.h>

TEST(SessionRepositoryIT, OneActiveSessionConstraint) {
    GTEST_SKIP() << "Requires running PostgreSQL";
}

TEST(SessionRepositoryIT, FindActive_ReturnsCorrectSession) {
    GTEST_SKIP() << "Requires running PostgreSQL";
}
